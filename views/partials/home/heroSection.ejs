<!-- ==================== HERO SECTION - FORM FULLY VISIBLE (NO SCROLL NEEDED) ==================== -->
<section id="hero" class="relative overflow-hidden flex items-center bg-black py-8 lg:py-12 min-h-[85vh] lg:min-h-[90vh]">
    
    <!-- ====== ANIMATED BACKGROUND WAVES (unchanged) ====== -->
    <div class="absolute inset-0 overflow-hidden">
        <div class="absolute w-[200%] h-[200%] top-[-50%] left-[-50%] animate-spin-slow opacity-30">
            <div class="absolute inset-0 bg-gradient-conic from-cyan-500 via-blue-500 to-purple-500 rounded-full blur-3xl"></div>
        </div>
    </div>
    
    <!-- ====== PULSING GLOW ORBS ====== -->
    <div class="absolute inset-0">
        <div class="absolute top-20 left-[10%] w-80 h-80 bg-cyan-500/30 rounded-full blur-3xl animate-ping-slow"></div>
        <div class="absolute bottom-20 right-[10%] w-96 h-96 bg-blue-500/30 rounded-full blur-3xl animate-pulse-glow" style="animation-delay: 1s;"></div>
        <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-purple-500/20 rounded-full blur-3xl animate-pulse-glow" style="animation-delay: 2s;"></div>
        <div class="absolute top-40 right-[20%] w-60 h-60 bg-teal-400/25 rounded-full blur-3xl animate-bounce-slow"></div>
    </div>
     
    <!-- ====== MOVING GRID LINES ====== -->
    <div class="absolute inset-0 opacity-[0.12] animate-grid-move"
        style="background-image: 
            repeating-linear-gradient(90deg, #22d3ee 0px, #22d3ee 2px, transparent 2px, transparent 60px),
            repeating-linear-gradient(0deg, #22d3ee 0px, #22d3ee 2px, transparent 2px, transparent 60px);">
    </div>
    
    <!-- ====== SECOND MOVING GRID ====== -->
    <div class="absolute inset-0 opacity-[0.08] animate-grid-move-reverse"
        style="background-image: 
            repeating-linear-gradient(90deg, #facc15 0px, #facc15 1px, transparent 1px, transparent 100px),
            repeating-linear-gradient(0deg, #facc15 0px, #facc15 1px, transparent 1px, transparent 100px);">
    </div>
    
    <!-- ====== FLOATING PARTICLES ====== -->
    <div class="absolute inset-0 overflow-hidden">
        <div class="particle particle-1"></div>
        <div class="particle particle-2"></div>
        <div class="particle particle-3"></div>
        <div class="particle particle-4"></div>
        <div class="particle particle-5"></div>
        <div class="particle particle-6"></div>
        <div class="particle particle-7"></div>
        <div class="particle particle-8"></div>
    </div>
   
    <!-- ====== GLOWING BORDER EFFECTS ====== -->
    <div class="absolute top-0 left-0 w-full h-full pointer-events-none">
        <div class="absolute top-10 left-10 w-40 h-40 border-l-2 border-t-2 border-cyan-400/50 rounded-tl-2xl animate-border-glow"></div>
        <div class="absolute bottom-10 right-10 w-40 h-40 border-r-2 border-b-2 border-blue-400/50 rounded-br-2xl animate-border-glow-delay"></div>
    </div>

    <div class="container mx-auto px-6 lg:px-12 relative z-10">

        <!-- Top Badge with Glow (fully visible) -->
        <div class="flex justify-center mb-6 lg:mb-8">
            <div class="inline-flex items-center gap-3 px-5 py-2 rounded-full bg-black/60 border border-cyan-500/50 shadow-lg backdrop-blur-sm hover:border-cyan-500/80 transition-all duration-300 animate-slide-down">
                <div class="w-8 h-8 rounded-full bg-cyan-500/30 flex items-center justify-center animate-pulse-ring">
                    <i class="fa-solid fa-briefcase text-cyan-400 text-sm"></i>
                </div>
                <div>
                    <h4 class="text-white font-bold text-sm">100% Placement Assistance</h4>
                    <p class="text-cyan-300/80 text-[11px]">Guaranteed Career Support</p>
                </div>
            </div>
        </div>

        <!-- Main Content - Two Columns, balanced height -->
        <div class="flex flex-col lg:flex-row gap-6 xl:gap-10 items-start">
            
            <!-- LEFT SIDE - CONTENT -->
            <div class="flex-1 text-center lg:text-left">
                
                <!-- Small Badge -->
                <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-cyan-500/15 border border-cyan-500/40 mb-4 animate-slide-down backdrop-blur-sm">
                    <span class="w-2.5 h-2.5 rounded-full bg-cyan-400 animate-pulse-ring"></span>
                    <span class="text-cyan-300 text-sm font-medium tracking-wide">
                        An Education Division of ASN Immigration Services Pvt. Ltd.
                    </span>
                </div>

                <!-- Heading -->
                <h1 class="text-3xl md:text-5xl lg:text-5xl xl:text-6xl font-extrabold text-white leading-[1.2] tracking-tight mb-4 animate-fade-up">
                    Bridging Skills with
                    <span class="text-3xl md:text-4xl bg-gradient-to-r from-cyan-400 via-blue-400 to-purple-400 bg-clip-text text-transparent animate-gradient-x">
                        Career Opportunities
                    </span>
                </h1>

                <!-- Dynamic Typing -->
                <div class="mt-4 mb-5 flex flex-wrap items-center justify-center lg:justify-start gap-2 animate-fade-up">
                    <div class="typing-wrapper">
                        <span class="typing-static text-gray-300 text-2xl md:text-3xl font-bold">Learn</span>
                        <span class="mx-1"></span>
                        <div class="typing-dynamic-container">
                            <span id="dynamic-word" class="typing-text text-cyan-400 text-2xl md:text-3xl font-black"></span>
                            <span id="dynamic-cursor" class="typing-cursor ml-0.5"></span>
                        </div>
                    </div>
                </div>

                <!-- Description in Box (slightly reduced padding) -->
                <div class="bg-white/5 backdrop-blur-sm border border-cyan-500/30 rounded-2xl p-4 mb-6 animate-fade-up">
                    <p class="text-gray-300 text-sm md:text-base leading-relaxed">
                        SkillBridge is the education division of ASN Immigration, offering IT skills, tech training, 
                        corporate learning, and online degree programs from UGC-approved and NAAC A+ accredited universities.
                    </p>
                </div>

                <!-- Stats with Animated Counters (more compact) -->
                <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 max-w-2xl mx-auto lg:mx-0 animate-fade-up">
                    <div class="bg-white/5 backdrop-blur-sm rounded-xl p-2 border border-white/10 hover:border-cyan-500/40 transition-all duration-300 hover:scale-105 group">
                        <div class="text-xl font-extrabold text-white group-hover:text-cyan-400 transition-colors">
                            <span class="counter" data-target="5000">0</span><span>+</span>
                        </div>
                        <p class="text-gray-400 text-xs mt-0.5">Students Trained</p>
                    </div>
                    <div class="bg-white/5 backdrop-blur-sm rounded-xl p-2 border border-white/10 hover:border-cyan-500/40 transition-all duration-300 hover:scale-105 group">
                        <div class="text-xl font-extrabold text-white group-hover:text-cyan-400 transition-colors">
                            <span class="counter" data-target="30">0</span><span>+</span>
                        </div>
                        <p class="text-gray-400 text-xs mt-0.5">Degree Programs</p>
                    </div>
                    <div class="bg-white/5 backdrop-blur-sm rounded-xl p-2 border border-white/10 hover:border-cyan-500/40 transition-all duration-300 hover:scale-105 group">
                        <div class="text-xl font-extrabold text-white group-hover:text-cyan-400 transition-colors">
                            <span class="counter" data-target="98">0</span><span>%</span>
                        </div>
                        <p class="text-gray-400 text-xs mt-0.5">Placement Rate</p>
                    </div>
                    <div class="bg-white/5 backdrop-blur-sm rounded-xl p-2 border border-white/10 hover:border-cyan-500/40 transition-all duration-300 hover:scale-105 group">
                        <div class="text-xl font-extrabold text-white group-hover:text-cyan-400 transition-colors">
                            <span class="counter" data-target="75">0</span><span>+</span>
                        </div>
                        <p class="text-gray-400 text-xs mt-0.5">Expert Mentors</p>
                    </div>
                </div>
            </div>

            <!-- RIGHT SIDE - ADMISSION INQUIRY FORM (compact, no overflow) -->
            <div style="position:relative; margin-top: -90px; margin-right:-40px;" class="flex-1 w-full max-w-md mx-auto lg:mx-0">
                <div class="bg-gradient-to-br from-gray-900/95 to-gray-900/95 backdrop-blur-md rounded-2xl p-4 sm:p-5 shadow-2xl border border-cyan-500/30">
                    <div class="flex items-center gap-2 mb-3">
                        <i class="fa-regular fa-pen-to-square text-cyan-400 text-lg animate-pulse"></i>
                        <h2 class="text-lg sm:text-xl font-bold text-white">Admission <span class="text-cyan-400">Inquiry</span></h2>
                    </div>
                
                    
                    <!-- Tabs (smaller) -->
                    <div class="flex gap-2 mb-3">
                        <button id="heroCoursesTab" class="hero-tab flex-1 px-2 py-1.5 rounded-xl bg-white/10 border border-gray-600 text-gray-300 font-semibold text-xs flex items-center justify-center gap-1 transition-all hover:bg-cyan-500/10 hover:border-cyan-500 active">
                            <i class="fa-solid fa-laptop-code"></i> <span>Courses</span>
                        </button>
                        <button id="heroDegreeTab" class="hero-tab flex-1 px-2 py-1.5 rounded-xl bg-white/10 border border-gray-600 text-gray-300 font-semibold text-xs flex items-center justify-center gap-1 transition-all hover:bg-cyan-500/10 hover:border-cyan-500">
                            <i class="fa-solid fa-graduation-cap"></i> <span>Degree Program</span>
                        </button>
                    </div>
                    
                    <form id="heroAdmissionForm" action="/submitEnquery" method="POST" class="space-y-2.5">
                        <div>
                            <label class="text-white text-xs font-semibold mb-0.5 block">Full Name <span class="text-red-500">*</span></label>
                            <input type="text" id="heroFullName" name="name" placeholder="Rahul Kumar" required 
                                   class="w-full px-3 py-1.5 rounded-xl bg-white/10 border border-gray-600 text-white placeholder-gray-400 focus:border-cyan-500 focus:ring-2 focus:ring-cyan-500/30 transition-all text-sm">
                        </div>
                        <div>
                            <label class="text-white text-xs font-semibold mb-0.5 block">Email Address <span class="text-red-500">*</span></label>
                            <input type="email" id="heroEmail" name="email" placeholder="hello@skillbridge.com" required 
                                   class="w-full px-3 py-1.5 rounded-xl bg-white/10 border border-gray-600 text-white placeholder-gray-400 focus:border-cyan-500 focus:ring-2 focus:ring-cyan-500/30 transition-all text-sm">
                        </div>
                        <div>
                            <label class="text-white text-xs font-semibold mb-0.5 block">Phone Number <span class="text-red-500">*</span></label>
                            <input type="tel" id="heroPhone" name="phoneNumber" placeholder="9876543210" required 
                                   class="w-full px-3 py-1.5 rounded-xl bg-white/10 border border-gray-600 text-white placeholder-gray-400 focus:border-cyan-500 focus:ring-2 focus:ring-cyan-500/30 transition-all text-sm">
                        </div>
                        
                        <div id="heroCourseOptions">
                            <label class="text-white text-xs font-semibold mb-0.5 block">Select Course <span class="text-red-500">*</span></label>
                            <select id="heroCourseSelect" name="course" required class="w-full px-3 py-1.5 rounded-xl bg-white/10 border border-gray-600 text-white focus:border-cyan-500 focus:ring-2 focus:ring-cyan-500/30 transition-all text-sm">
                                <option value="" disabled selected class="text-gray-900 bg-white">Select a course</option>
                                <option class="text-gray-900 bg-white">Web Development</option>
                                <option class="text-gray-900 bg-white">Generative AI</option>
                                <option class="text-gray-900 bg-white">Data Science & AI</option>
                                <option class="text-gray-900 bg-white">Data Analytics</option>
                                <option class="text-gray-900 bg-white">Digital Marketing</option>
                                <option class="text-gray-900 bg-white">Programming Language</option>
                                <option class="text-gray-900 bg-white">UI/UX Design</option>
                                <option class="text-gray-900 bg-white">Data Structures & Algorithms</option>
                                <option class="text-gray-900 bg-white">Placement Training</option>
                            </select>
                        </div>
                        
                        <div id="heroDegreeOptions" class="hidden">
                            <label class="text-white text-xs font-semibold mb-0.5 block">Select Degree Program <span class="text-red-500">*</span></label>
                            <select id="heroDegreeSelect" name="course" required class="w-full px-3 py-1.5 rounded-xl bg-white/10 border border-gray-600 text-white focus:border-cyan-500 focus:ring-2 focus:ring-cyan-500/30 transition-all text-sm">
                                <option value="" disabled selected class="text-gray-900 bg-white">Select an online degree program</option>
                                <option class="text-gray-900 bg-white">MBA</option>
                                <option class="text-gray-900 bg-white">MCA</option>
                                <option class="text-gray-900 bg-white">M.Sc</option>
                                <option class="text-gray-900 bg-white">M.Com</option>
                                <option class="text-gray-900 bg-white">M.Ed & Ed.D</option>
                                <option class="text-gray-900 bg-white">PGDM</option>
                                <option class="text-gray-900 bg-white">BCA</option>
                                <option class="text-gray-900 bg-white">B.Sc</option>
                                <option class="text-gray-900 bg-white">BBA</option>
                                <option class="text-gray-900 bg-white">BA</option>
                            </select>
                        </div>
                        
                        <!-- Message optional -->
                        <div>
                            <label class="text-white text-xs font-semibold mb-0.5 block">Message / Query (Optional)</label>
                            <textarea id="heroMessage" name="message" rows="1" placeholder="I'm interested in...courses, online degrees, career guidance"
                                      class="w-full px-3 py-1.5 rounded-xl bg-white/10 border border-gray-600 text-white placeholder-gray-400 focus:border-cyan-500 focus:ring-2 focus:ring-cyan-500/30 transition-all resize-none text-sm"></textarea>
                        </div>
                        
                        <button type="submit" class="w-full bg-gradient-to-r from-cyan-500 to-blue-600 hover:from-cyan-600 hover:to-blue-700 text-white py-2 rounded-xl font-semibold transition-all hover:scale-[1.02] flex items-center justify-center gap-2 group text-sm">
                            Submit Inquiry <i class="fa-solid fa-paper-plane group-hover:translate-x-1 transition-transform"></i>
                        </button>
                    </form>
                 
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    /* ====== ALL EXISTING ANIMATIONS REMAIN THE SAME (unchanged) ====== */
    @keyframes spin-slow { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
    .animate-spin-slow { animation: spin-slow 15s linear infinite; }
    @keyframes pulse-glow { 0%, 100% { opacity: 0.15; transform: scale(1); } 50% { opacity: 0.4; transform: scale(1.1); } }
    .animate-pulse-glow { animation: pulse-glow 3s ease-in-out infinite; }
    @keyframes ping-slow { 0%, 100% { opacity: 0.2; transform: scale(1); } 50% { opacity: 0.6; transform: scale(1.2); } }
    .animate-ping-slow { animation: ping-slow 2s ease-in-out infinite; }
    @keyframes bounce-slow { 0%, 100% { transform: translateY(0px); } 50% { transform: translateY(-20px); } }
    .animate-bounce-slow { animation: bounce-slow 3s ease-in-out infinite; }
    @keyframes grid-move { 0% { background-position: 0 0; } 100% { background-position: 60px 0; } }
    .animate-grid-move { animation: grid-move 10s linear infinite; }
    @keyframes grid-move-reverse { 0% { background-position: 0 0; } 100% { background-position: -100px 0; } }
    .animate-grid-move-reverse { animation: grid-move-reverse 15s linear infinite; }
    @keyframes border-glow { 0%, 100% { border-color: rgba(34,211,238,0.3); box-shadow: 0 0 10px rgba(34,211,238,0); } 50% { border-color: rgba(34,211,238,1); box-shadow: 0 0 30px rgba(34,211,238,0.5); } }
    .animate-border-glow { animation: border-glow 2s ease-in-out infinite; }
    .animate-border-glow-delay { animation: border-glow 2.5s ease-in-out infinite 1s; }
    @keyframes fade-up { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
    .animate-fade-up { animation: fade-up 0.6s ease-out forwards; }
    @keyframes slide-down { from { opacity: 0; transform: translateY(-30px); } to { opacity: 1; transform: translateY(0); } }
    .animate-slide-down { animation: slide-down 0.5s ease-out forwards; }
    @keyframes gradient-x { 0%, 100% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } }
    .animate-gradient-x { background-size: 200% auto; animation: gradient-x 3s ease-in-out infinite; }
    @keyframes pulse-ring { 0%, 100% { opacity: 1; transform: scale(1); } 50% { opacity: 0.5; transform: scale(1.3); } }
    .animate-pulse-ring { animation: pulse-ring 1.5s ease-in-out infinite; }
    .particle { position: absolute; background: rgba(34,211,238,0.6); border-radius: 50%; animation: float-particle 8s infinite ease-in-out; }
    .particle-1 { width: 4px; height: 4px; top: 15%; left: 20%; animation-duration: 6s; }
    .particle-2 { width: 6px; height: 6px; top: 70%; left: 85%; animation-duration: 8s; animation-delay: 1s; }
    .particle-3 { width: 3px; height: 3px; top: 40%; left: 10%; animation-duration: 5s; animation-delay: 2s; }
    .particle-4 { width: 5px; height: 5px; top: 80%; left: 30%; animation-duration: 7s; animation-delay: 0.5s; }
    .particle-5 { width: 4px; height: 4px; top: 25%; left: 75%; animation-duration: 9s; animation-delay: 1.5s; }
    .particle-6 { width: 6px; height: 6px; top: 55%; left: 50%; animation-duration: 6.5s; animation-delay: 3s; }
    .particle-7 { width: 3px; height: 3px; top: 10%; left: 45%; animation-duration: 7.5s; animation-delay: 2.5s; }
    .particle-8 { width: 5px; height: 5px; top: 90%; left: 60%; animation-duration: 5.5s; animation-delay: 1.8s; }
    @keyframes float-particle { 0%, 100% { transform: translateY(0px) translateX(0px); opacity: 0.6; } 25% { transform: translateY(-30px) translateX(15px); opacity: 1; } 50% { transform: translateY(10px) translateX(-10px); opacity: 0.4; } 75% { transform: translateY(-15px) translateX(20px); opacity: 0.8; } }
    .bg-gradient-conic { background: conic-gradient(from 0deg, #06b6d4, #3b82f6, #8b5cf6, #06b6d4); }
    
    .typing-wrapper { display: inline-flex; align-items: center; gap: 4px; background: transparent; }
    .typing-static { color: #e5e7eb; font-weight: 700; }
    .typing-dynamic-container { display: inline-flex; align-items: center; position: relative; min-width: 240px; }
    .typing-text { color: #22d3ee; font-weight: 900; white-space: nowrap; display: inline-block; letter-spacing: 1px; text-shadow: 0 0 8px rgba(34,211,238,0.6); }
    .typing-cursor { display: inline-block; width: 3px; height: 2rem; background-color: #22d3ee; vertical-align: middle; margin-left: 4px; border-radius: 2px; opacity: 1; box-shadow: 0 0 4px #22d3ee; }
    
    .hero-tab.active { background: rgba(34,211,238,0.2); border-color: #22d3ee; color: #22d3ee; }
    
    @media (max-width: 1024px) {
        .typing-dynamic-container { min-width: 200px; }
        .typing-text, .typing-static { font-size: 1.8rem; }
        .typing-cursor { height: 1.8rem; }
    }
    @media (max-width: 768px) {
        .typing-dynamic-container { min-width: 180px; }
        .typing-text, .typing-static { font-size: 1.4rem; }
        .typing-cursor { height: 1.5rem; }
        #hero { min-height: auto; padding-bottom: 2rem; }
    }
</style>

<script>
    // ==================== DYNAMIC TYPING ANIMATION ====================
    (function() {
        const words = ["Web Development ", "Data Science ", "DSA ", "Data Analytics ", "Digital Marketing ", "Programming Languages ", "UI/UX Design ", "Placement Training "];
        let wordIndex = 0, charIndex = 0, isDeleting = false, isWaiting = false, timeoutId = null;
        const textElement = document.getElementById('dynamic-word');
        if (!textElement) return;
        
        function clearTypingTimeout() { if (timeoutId) { clearTimeout(timeoutId); timeoutId = null; } }
        
        function typeEffect() {
            clearTypingTimeout();
            if (isWaiting) {
                timeoutId = setTimeout(() => { isWaiting = false; typeEffect(); }, 200);
                return;
            }
            const currentWord = words[wordIndex];
            if (isDeleting) {
                if (charIndex > 0) { charIndex--; textElement.textContent = currentWord.substring(0, charIndex); }
                if (charIndex === 0) { isDeleting = false; wordIndex = (wordIndex + 1) % words.length; isWaiting = true; timeoutId = setTimeout(typeEffect, 800); return; }
            } else {
                if (charIndex < currentWord.length) { charIndex++; textElement.textContent = currentWord.substring(0, charIndex); }
                if (charIndex === currentWord.length) { isDeleting = true; isWaiting = true; timeoutId = setTimeout(typeEffect, 2000); return; }
            }
            const speed = isDeleting ? 35 : 65;
            timeoutId = setTimeout(typeEffect, speed);
        }
        setTimeout(typeEffect, 300);
    })();

    // ==================== COUNTER ANIMATION (IMMEDIATE ON PAGE LOAD) ====================
    (function() {
        function animateCounter(element, target) {
            let current = 0;
            const increment = target / 50;
            const timer = setInterval(() => {
                current += increment;
                if (current >= target) {
                    element.textContent = Math.floor(target);
                    clearInterval(timer);
                } else {
                    element.textContent = Math.floor(current);
                }
            }, 20);
        }
        const counters = document.querySelectorAll('.counter');
        counters.forEach(counter => {
            const target = parseInt(counter.getAttribute('data-target'));
            if (!isNaN(target)) animateCounter(counter, target);
        });
    })();

    // ==================== HERO FORM TAB LOGIC & SUBMISSION ====================
    document.addEventListener('DOMContentLoaded', function() {
        const coursesTab = document.getElementById('heroCoursesTab');
        const degreeTab = document.getElementById('heroDegreeTab');
        const courseOptions = document.getElementById('heroCourseOptions');
        const degreeOptions = document.getElementById('heroDegreeOptions');
        const heroForm = document.getElementById('heroAdmissionForm');
        
        if (!coursesTab || !degreeTab) return;
        
        function activateTab(tabToActivate, showElement, hideElement) {
            coursesTab.classList.remove('active');
            degreeTab.classList.remove('active');
            tabToActivate.classList.add('active');
            showElement.classList.remove('hidden');
            hideElement.classList.add('hidden');
        }
        
        coursesTab.addEventListener('click', function(e) {
            e.preventDefault();
            activateTab(coursesTab, courseOptions, degreeOptions);
        });
        
        degreeTab.addEventListener('click', function(e) {
            e.preventDefault();
            activateTab(degreeTab, degreeOptions, courseOptions);
        });
        
        activateTab(coursesTab, courseOptions, degreeOptions);
        
        if (heroForm) {
            heroForm.addEventListener('submit', async function(e) {
                e.preventDefault();
                
                const name = document.getElementById('heroFullName')?.value.trim();
                const email = document.getElementById('heroEmail')?.value.trim();
                const phone = document.getElementById('heroPhone')?.value.trim();
                const message = document.getElementById('heroMessage')?.value.trim() || '';
                
                const isCourseTab = coursesTab.classList.contains('active');
                let courseValue = '';
                if (isCourseTab) {
                    const courseSelect = document.getElementById('heroCourseSelect');
                    courseValue = courseSelect?.value;
                } else {
                    const degreeSelect = document.getElementById('heroDegreeSelect');
                    courseValue = degreeSelect?.value;
                }
                
                if (!name || !email || !phone || !courseValue) {
                    alert('Please fill all required fields.');
                    return;
                }
                
                const payload = { name, email, phoneNumber: phone, course: courseValue, message };
                const submitBtn = heroForm.querySelector('button[type="submit"]');
                const originalText = submitBtn.innerHTML;
                submitBtn.disabled = true;
                submitBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Sending...';
                
                try {
                    const response = await fetch('/submitEnquery', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(payload)
                    });
                    const result = await response.json();
                    if (response.ok) {
                        alert('✅ Inquiry sent! You will receive a confirmation email shortly.');
                        heroForm.reset();
                        document.getElementById('heroCourseSelect').value = '';
                        document.getElementById('heroDegreeSelect').value = '';
                        activateTab(coursesTab, courseOptions, degreeOptions);
                    } else {
                        alert('❌ ' + (result.error || 'Submission failed. Please try again.'));
                    }
                } catch(err) {
                    console.error('Submission error:', err);
                    alert('❌ Something went wrong. Please try again later.');
                } finally {
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = originalText;
                }
            });
        }
    });
</script>